SNTG-D
------------
-update logs
	- frequency is set by cfg file, (Default every 1 min)
	- format based on plugins loger function??
		- string log( Status ) ??
		- void log( Status/String, logFile ) ??
	-what to log?
		-alert types
			- status update
			- message received/sent
			- timed event????? ( have sntg alert you when someone logs 
					     into fb? can you even do this with API ??)
	-log file entree setup:
		[date::time] <entree type> {info}
		[time - date] <importance> {entree type} "data"
		[date::time]<pluginName>{functionName}::"data"

-alerts
	- costomizable (based on plugin updates/logs/cfg)!!!
	- how to alert
		- internal email, like Cron job.
		- play a tune or sound (different for each plugin/alert-type)

-read and interpret config files
	- config file setup:
		...
		[main]
		alert_time=1 #this means 1 minute
		main_dir="/home/darkstar/.sntg/."

		[plugin_facebook]
		blahblahblah=42
		...

-run functions
	- internal
		- add/remove plugins (could also be done in the config file)
		- restart/stop/start daemon

	- external !!!DONE BY SNTG, NOT THE DAEMON!!!
		- run a function in a plugin
			-example:  sntg -pfb --update-status="hi there, im using sntg!!"
		- list all plugins
			-list functions (help) for each plugin


----------------------------------------------------------------------

import everything


if sntg-d is not running
	print "Uh Oh!! The SNTG Daemon has not been started! Start it? (Y/n/c)"
	if( enter or "y" ) // start the daemon before running the command
		start daemon
	else if( "c" ) // then cancel the command
		exit all
	else // 	











-=-=-=-=-=--=-=-
links:

Heres a link that will help create our package:
http://www.rexx.com/~dkuhlman/python_201/python_201.html#SECTION009000000000000000000

daemon example thats good:
http://code.activestate.com/recipes/278731-creating-a-daemon-the-python-way/













